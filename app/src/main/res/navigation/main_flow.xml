<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/filmsNavigation"
    app:startDestination="@+id/navigationMovies">

    <fragment
        android:id="@+id/navigationMovies"
        android:name="com.diwixis.feature_movie_list.presentation.MovieListPagerFragment"
        tools:layout="@layout/fragment_movie_list"/>

    <fragment
        android:id="@+id/navigationCustomView"
        android:name="com.diwixis.CustomViewFragment"
        tools:layout="@layout/fragment_custom_view"/>

    <fragment
        android:id="@+id/navigationAbout"
        android:name="com.diwixis.about.AboutFragment"
        tools:layout="@layout/fragment_about" />

</navigation>